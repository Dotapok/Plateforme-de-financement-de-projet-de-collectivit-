import React from 'react'; import { Shield, Building2, Award, Globe } from 'lucide-react'; interface GovernmentSealProps { size?: 'sm' | 'md' | 'lg' | 'xl'; variant?: 'default' | 'building' | 'award' | 'globe'; animated?: boolean; className?: string; } export function GovernmentSeal({ size = 'md', variant = 'default', animated = true, className = '' }: GovernmentSealProps) { const sizeClasses = { sm: 'w-8 h-8', md: 'w-12 h-12', lg: 'w-16 h-16', xl: 'w-20 h-20' }; const iconSizes = { sm: 'h-4 w-4', md: 'h-6 w-6', lg: 'h-8 w-8', xl: 'h-10 w-10' }; const getIcon = () => { switch (variant) { case 'building': return <Building2 className={iconSizes[size]} />; case 'award': return <Award className={iconSizes[size]} />; case 'globe': return <Globe className={iconSizes[size]} />; default: return <Shield className={iconSizes[size]} />; } }; const getGradient = () => { switch (variant) { case 'building': return 'from-blue-600 to-blue-800'; case 'award': return 'from-yellow-500 to-yellow-700'; case 'globe': return 'from-green-600 to-green-800'; default: return 'from-yellow-500 to-yellow-700'; } }; return ( <div className={` gov-seal ${sizeClasses[size]} bg-gradient-to-br ${getGradient()} border-2 border-blue-800 shadow-lg ${animated ? 'animate-float' : ''} ${className} `} style={{ animationDelay: animated ? `${Math.random() * 2}s` : '0s' }} > {getIcon()} </div> ); } // Composant de sceau avec drapeau camerounais export function CameroonianSeal({ size = 'md', className = '' }: { size?: 'sm' | 'md' | 'lg' | 'xl'; className?: string }) { const sizeClasses = { sm: 'w-8 h-6', md: 'w-12 h-8', lg: 'w-16 h-10', xl: 'w-20 h-12' }; return ( <div className={`relative ${className}`}> <div className={`${sizeClasses[size]} bg-gradient-to-r from-green-600 via-red-500 to-yellow-400 rounded-lg shadow-lg border-2 border-white`}> <div className="absolute inset-0 flex items-center justify-center"> <div className={`${size === 'sm' ? 'w-1 h-1' : size === 'md' ? 'w-2 h-2' : size === 'lg' ? 'w-3 h-3' : 'w-4 h-4'} bg-yellow-400 rounded-full`}></div> </div> </div> </div> ); } // Composant de badge gouvernemental export function GovernmentBadge({ children, variant = 'default', className = '' }: { children: React.ReactNode; variant?: 'default' | 'success' | 'warning' | 'info'; className?: string; }) { const variantClasses = { default: 'bg-yellow-500 text-white', success: 'bg-green-500 text-white', warning: 'bg-orange-500 text-white', info: 'bg-blue-500 text-white' }; return ( <span className={` gov-badge ${variantClasses[variant]} ${className} `}> {children} </span> ); } // Composant de carte gouvernementale export function GovernmentCard({ children, hover = true, className = '' }: { children: React.ReactNode; hover?: boolean; className?: string; }) { return ( <div className={` gov-card ${hover ? 'hover:scale-105' : ''} ${className} `}> {children} </div> ); } 
